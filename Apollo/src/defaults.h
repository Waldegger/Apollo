#pragma once

#include "graphics/texture.h"
#include "graphics/shader_program.h"

namespace agl
{
	namespace defaults
	{
		inline const texture* default_texture = nullptr;
		inline const shader_program* default_shader_program = nullptr;
	}
}